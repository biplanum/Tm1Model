{
	"@type":"Hierarchy",
	"Name":"Git credential",
	"Elements":
	[
		{
			"Name":"Username: the Git credential.",
			"Type":"Numeric"
		},
		{
			"Name":"Password: the Git credential. When neither Username nor Password is specified, the server does not verify the connection to the Git. Otherwise, the connection must be valid.",
			"Type":"Numeric"
		},
		{
			"Name":"Force: whether to reset the Git context.",
			"Type":"Numeric"
		},
		{
			"Name":"URL: the URL of the Git repository. When present, the URL MUST match the URL in the Git context, if the Git context is not being reset.",
			"Type":"Numeric"
		},
		{
			"Name":"Deployment: the selected deployment.",
			"Type":"Numeric"
		}
	],
	"Subsets@Code.links":
	[
		"Git credential.subsets/SubMarks.json",
		"Git credential.subsets/Subset1.json"
	]
}